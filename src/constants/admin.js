/**
 * Default admin user for backend seeding and login.
 * Backend must create this user with role ADMIN (or isAdmin: true).
 */
export const DEFAULT_ADMIN_USER = {
  email: "admin@inxinfo.com",
  password: "Admin@123",
};
