# Backend API base URL. Use the same base as your Swagger/backend.
# With gateway or monolith (context-path /api): http://localhost:8080/api
# If backend has no /api prefix (e.g. auth on 8080 only): http://localhost:8080
REACT_APP_API_URL=http://localhost:8080/api

# Google OAuth2 client ID for "Sign in with Google" (optional; from Google Cloud Console)
REACT_APP_GOOGLE_CLIENT_ID=
