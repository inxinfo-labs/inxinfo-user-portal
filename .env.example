# Backend API base URL (inxinfo-auth-service). The backend must be running or you'll see "Cannot reach the server".
# Start the backend first, then use the same base as your Swagger/backend.
# With context-path /api: http://localhost:8080/api
# If backend runs on a different host/port, set that here (e.g. https://your-api.com/api).
REACT_APP_API_URL=http://localhost:8080/api

# Google OAuth2 client ID for "Sign in with Google" (optional; from Google Cloud Console)
REACT_APP_GOOGLE_CLIENT_ID=

# --- Contact form: deliver messages to satish.prasad@inxinfo.com (optional but recommended) ---
# Get these from https://www.emailjs.com/ (free tier). Create a service (e.g. Gmail), then a template.
# In the template set To: satish.prasad@inxinfo.com and use variables: {{from_name}} {{from_email}} {{subject}} {{message}}
# REACT_APP_EMAILJS_SERVICE_ID=your_service_id
# REACT_APP_EMAILJS_TEMPLATE_ID=your_template_id
# REACT_APP_EMAILJS_PUBLIC_KEY=your_public_key
